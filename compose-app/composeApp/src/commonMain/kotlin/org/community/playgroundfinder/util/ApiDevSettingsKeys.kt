package org.community.playgroundfinder.util

/** Debug-only: persisted override for API base URL (see DevApiEndpointScreen). */
object ApiDevSettingsKeys {
    const val SERVER_BASE_URL_OVERRIDE = "dev_server_base_url_override"
}
